# Copy to .smtp.env. SMTP password/key for alert emails (must match password_env in config/rules.yaml).
# Leave empty if running email in DRY-RUN. Gmail: 16-char App Password. Brevo: the SMTP key.
WATCHER_SMTP_PASSWORD=
